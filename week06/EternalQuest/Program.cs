public class Program
{
    public static void Main(string[] args)
    {
        // Only a code calling the method which starts the program
        GoalManager manager = new GoalManager();
        manager.Start();
    }
}